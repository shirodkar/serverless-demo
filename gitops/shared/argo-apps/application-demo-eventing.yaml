apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: demo-eventing
  namespace: shared-gitops
  labels:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  template:
    metadata:
      name: demo-eventing-{{name}}
    spec:
      destination:
        namespace: demo-eventing
        server: "{{server}}"
      project: default
      source:
        path: gitops/shared/demo-apps/eventing-demo
        repoURL: 'https://github.com/shirodkar/serverless-demo.git'
        targetRevision: HEAD
        directory:
          recurse: true
      syncPolicy:
        automated:
          enabled: true
          prune: true
          selfHeal: true