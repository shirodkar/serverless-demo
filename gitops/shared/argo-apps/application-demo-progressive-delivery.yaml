apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: demo-progressive-delivery
  namespace: shared-gitops
  labels:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  generators:
    - clusterDecisionResource:
        configMapRef: acm-placement
        labelSelector:
          matchLabels:
            cluster.open-cluster-management.io/placement: dev-only-placement
        requeueAfterSeconds: 180
  template:
    metadata:
      name: demo-progressive-delivery-{{name}}
    spec:
      destination:
        namespace: demo-progressive-delivery
        server: "{{server}}"
      project: default
      source:
        path: gitops/shared/demo-apps/progressive-delivery-demo
        repoURL: 'https://github.com/shirodkar/serverless-demo.git'
        targetRevision: HEAD
        directory:
          recurse: true
      syncPolicy:
        automated:
          enabled: true
          prune: true
          selfHeal: true