apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-progressive-delivery
  namespace: openshift-gitops
  labels:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: demo-progressive-delivery
    server: 'https://kubernetes.default.svc'
  project: default
  source:
    path: gitops/demo-apps/progressive-delivery-demo
    repoURL: 'https://github.com/shirodkar/serverless-demo.git'
    targetRevision: HEAD
    directory:
      recurse: true
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true