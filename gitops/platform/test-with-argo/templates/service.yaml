apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-with-argo.fullname" . }}
  labels:
    {{- include "test-with-argo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-with-argo.selectorLabels" . | nindent 4 }}
