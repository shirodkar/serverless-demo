apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-with-argo.fullname" . }}-test-connection"
  labels:
    {{- include "test-with-argo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-with-argo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
